# Compile these requirements with pip-tools like so:
#
#    $ pip install pip-tools
#    $ pip-compile --output-file=requirements.txt requirements.in
#
# This is useful for resolving complicated dependencies.

flake8
SQLAlchemy
pybigquery
pymysql
psycopg2
pip-tools
pytest
retrying
sqlalchemy-redshift
setuptools
timeout-decorator